{% extends 'base.html.twig' %}

{% block title %}Employes{% endblock %}

{% block body %}
    <h1>Employes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employe.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ employe.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ employe.roles ? employe.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ employe.password }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ employe.username }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('employes_index') }}">back to list</a>

    <a href="{{ path('employes_edit', {'id': employe.id}) }}">edit</a>

    {{ include('employes/_delete_form.html.twig') }}
{% endblock %}
