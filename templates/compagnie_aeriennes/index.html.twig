{% extends 'base.html.twig' %}

{% block title %}CompagnieAeriennes index{% endblock %}

{% block body %}
    <h1>CompagnieAeriennes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Raison</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compagnie_aerienne in compagnie_aeriennes %}
            <tr>
                <td>{{ compagnie_aerienne.id }}</td>
                <td>{{ compagnie_aerienne.raison }}</td>
                <td>
                    <a href="{{ path('compagnie_aeriennes_show', {'id': compagnie_aerienne.id}) }}">show</a>
                    <a href="{{ path('compagnie_aeriennes_edit', {'id': compagnie_aerienne.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('compagnie_aeriennes_new') }}">Create new</a>
{% endblock %}
