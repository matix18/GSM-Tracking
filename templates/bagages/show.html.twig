{% extends 'base.html.twig' %}

{% block title %}Bagages{% endblock %}

{% block body %}
    <h1>Bagages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bagage.id }}</td>
            </tr>
            <tr>
                <th>CodeBagage</th>
                <td>{{ bagage.codeBagage }}</td>
            </tr>
            <tr>
                <th>NombreBagage</th>
                <td>{{ bagage.nombreBagage }}</td>
            </tr>
            <tr>
                <th>ListeBagages</th>
                <td>{{ bagage.listeBagages }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bagages_index') }}">back to list</a>

    <a href="{{ path('bagages_edit', {'id': bagage.id}) }}">edit</a>

    {{ include('bagages/_delete_form.html.twig') }}
{% endblock %}
